# ANS Prospect Tool — Runtime toggles only.
# ALL secrets (Anthropic API key, M365 credentials) live in macOS Keychain.
# Enter them via /admin/credentials after first run. Never put secrets here.

APP_HOST=127.0.0.1
APP_PORT=8765
LOG_LEVEL=INFO
