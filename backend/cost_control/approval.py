"""Cost approval gate shown to the user before Stage 2 runs.

Builds per-agent cost breakdown, total estimate, and remaining budgets
for display in the briefing review UI. The user must explicitly approve
before Stage 2 starts; this module provides the data, not the UI.

Implemented in step 9 (Sonnet — UI; Opus for state machine).
"""
# TODO: implement in step 9
