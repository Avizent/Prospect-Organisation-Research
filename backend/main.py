"""FastAPI application entry point.

Mounts all routers, configures middleware, probes for Ghostscript/Poppler
at startup, and enforces the concurrent-job semaphore.

Implemented in step 2 (DB) and step 3 (auth routes), completed in step 16.
"""
# TODO: implement in steps 2-16
