"""Product mapping agent — Stage 2, step 1.

Reads approved briefing + ANS knowledge base. Produces need→product mappings
with concrete use-case framing. Pre-filters which knowledge base excerpts
each downstream writer needs.

Implemented in step 10 (Opus — subtle reasoning required).
"""
# TODO: implement in step 10
