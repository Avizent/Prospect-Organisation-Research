"""Research agent — Stage 1, step 1.

Uses Anthropic web_search tool to gather intelligence on the target company
(15-20 queries). Priorities: job postings, annual reports, regulatory exposure,
vendor footprint, M&A, public network incidents, senior infra hires.

Output: structured JSON dossier written to jobs/{job_id}/research_dossier.json

Implemented in step 6 (Sonnet — well-specified on top of base wrapper).
"""
# TODO: implement in step 6
